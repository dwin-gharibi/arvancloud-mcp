apiVersion: v1
kind: Service
metadata:
  name: arvancloud-mcp
  namespace: arvancloud-mcp
  labels:
    app.kubernetes.io/name: arvancloud-mcp
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: arvancloud-mcp
  ports:
    - name: http
      port: 80
      targetPort: http
