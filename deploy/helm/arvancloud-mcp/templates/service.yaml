apiVersion: v1
kind: Service
metadata:
  name: {{ include "arvancloud-mcp.fullname" . }}
  labels:
    {{- include "arvancloud-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "arvancloud-mcp.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
